import styled from "styled-components"

const StyledSection = styled.div`
    display: flex;
    flex-direction: column;
    gap: 10px;
`

export const OrderModalSection = () => {
    return (
        <div></div>
    )
}